{
  "name": "ts-react-esbuild",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm run --parallel --aggregate-output --filter client --filter server dev",
    "build": "pnpm run --parallel --aggregate-output --filter client --filter server build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.0",
    "@typescript-eslint/eslint-plugin": "^5.59.5",
    "esbuild": "^0.17.18",
    "rome": "^12.0.0",
    "typescript": "^5.0.4"
  }
}
